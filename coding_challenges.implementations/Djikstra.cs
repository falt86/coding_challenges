using System;
using System.Collections.Generic;
using System.Text;

namespace coding_challenges.implementations
{
    public class Djikstra
    {
        /// <summary>
        /// 
        /// </summary>
        /// <param name="graph">an array of arrays, that list the distances from each point to every other point</param>
        /// <example>2 points 1 unit apart would be [[0,1],[1,0]]</example>
        public void Solve(int[,] graph)
        {
            
        }
    }
}
